# Yiora 社区 · 交付总纲

> 版本:v1.2(2026-07)  范围:后端 API + WS 网关 + 管理后台前后端 + 交付基线
> 需求依据:`docs/Yiora开发需求文档.md`(M2 社区内核 / M3 软件库与忧珠 / M4 商城付费 / P0-P2 细节 / 3.12 管理后台)
> v1.1:对象存储直传 + 图片域名白名单 + 后台 TOTP;v1.2:双令牌设备体系 + 运营/商城/合规管理页补齐,管理后台迁移 vue-pure-admin 模板

## 一、总体架构

```mermaid
flowchart LR
    subgraph Client
        APP[Flutter 客户端\napp/ 进行中]
        ADMIN[管理后台 admin-web\nVue3 + Element Plus]
    end
    subgraph Server["server/ (Go · go-zero 模块化单体)"]
        API[yiora-api :8888\n/api/v1 + /admin/v1]
        WS[yiora-ws :8889\nWebSocket 网关]
    end
    MYSQL[(MySQL 8.0\n40+ 表 · 10 份迁移)]
    REDIS[(Redis 7\n缓存/限流/黑名单/验证码)]

    APP -->|REST| API
    APP -->|ws://.../ws?token| WS
    ADMIN -->|/admin/v1| API
    API -->|内部推送 /internal/push| WS
    API --> MYSQL
    API --> REDIS
    WS --> REDIS
```

- **分层**:handler(路由/鉴权)→ logic(业务)→ model(SQL/事务),DTO 统一在 `internal/types`。
- **鉴权**:用户侧双令牌 —— access JWT(配置驱动,含设备 claim)+ refresh token(30 天,轮换式,一次性);
  设备体系(上限 5 台自动淘汰、单设备踢下线即时生效);注销/封禁/踢设备经 Redis 吊销标记拦存量 token。
  管理 JWT(8 小时,claim 隔离,RBAC 权限码,验证码+错密锁定+IP 白名单+TOTP 二步验证)。
- **一致性**:资金路径(忧珠)全事务 + 幂等 biz_key + 死锁重试;计数增减随状态流转同事务回补。
- **对象存储**:S3 兼容预签名直传(dev 用 compose 内 MinIO),业务图片强制我方域名白名单;
  APK 走 S3 Multipart 分片直传(8MB/片,上限 2GB,断点续传 parts 比对补签,uploadId 归属票据防越权)。
- **图片机审**:imgscan 抽象层(pass/review/block 三态),发帖/编辑异步送审——review 进人审队列、
  block 自动驳回+通知作者;头像/封面保存前同步拦截;驱动可插拔(mock 已内置,腾讯 IMS 二期填密钥即接)。

## 二、模块清单

| 模块 | 位置 | 要点 |
| --- | --- | --- |
| 账号 | authlogic / userlogic | 邮箱注册登录、找回、注销(token 吊销)、资料、关注/拉黑、足迹/收藏 |
| 社区 | postlogic / commentlogic / circlelogic | 发帖(话题/共创/付费段)、编辑重审、评论(帖/软件多业务对象)、圈子(圈主置顶/加精/禁言/删帖) |
| 私信 | imlogic + ws | 会话/消息/撤回(2 分钟,DB 时钟)、拉黑、AI 管家(FAQ 规则应答+新人欢迎) |
| 软件库 | softwarelogic | 发布(人审)、版本、分类/标签、下载计数 |
| 忧珠经济 | tasklogic / youzhulogic / malllogic | 任务/签到、账本(幂等)、装扮/靓号/抽奖、付费解锁帖(作者分成)、每日对账 |
| 搜索 | searchmodel + pkg/search(meili) | 双驱动配置切换:MySQL LIKE(默认)/ Meilisearch(中文分词/错字容忍,水位增量同步 daemon,故障单查降级 LIKE) |
| 内容安全 | sensitive + audit_queue + imgscan | 三级词库(拦截/人审/打码,后台热更新)、图片机审三态(mock 驱动,云驱动可插拔)、人审工作台 |
| 管理后台 | adminlogic + admin-web | 见下节 |

## 三、管理后台(20 页 · /admin/v1 60+ 接口,vue-pure-admin 模板)

登录安全:图形验证码(SVG 自绘,Redis 一次性)→ 同账号 5 次错密锁 15 分钟 → IP 白名单(`Admin.IPAllowlist`)→ TOTP 二步验证(RFC 6238,绑定确认制+10 个一次性恢复码,超管可强制解绑)。

| 页面 | 能力 |
| --- | --- |
| 数据看板 | 问候条+待审 CTA、八项指标卡片、ECharts 近 7/30/90 日注册·发帖·忧珠四曲线(按日补零) |
| 审核工作台 | 帖/评/软件机审队列,图片机审标签/命中图/置信度结构化展示,过审计数补记,驳回必填原因,CAS 防重,通知作者 |
| 内容管理 | 帖/评关键词+全状态检索,一键下架/恢复(计数自动回补),帖子首页置顶/加精开关 |
| 举报处理 | 待处理 FIFO+目标摘要回查,弹窗内快捷下架/禁言封禁,结单通知举报人 |
| 认证审核 | 达人/开发者认证裁决+徽章 |
| 话题管理 | 封禁/恢复、热度调整 |
| 用户管理 | 搜索筛选、禁言/封禁/恢复(存量 token 受控)、等级调整、头衔授予/撤销 |
| 圈子管理 | 创建/编辑(图标封面直传)、官方标记、发现页置顶排序、隐藏解散、圈主任命 |
| Banner / 公告 | 定时投放(图片直传);公告单 SQL 全员扇出系统通知 |
| 忧珠运营 | 手动发放/回收(幂等账本+通知+二次确认)、全站流水查询(UID/类型筛选) |
| 商城/任务配置 | 装扮·奖池·任务·靓号库四 tab CRUD(已售靓号锁定;status 上下架不物理删) |
| 软件分类 | 应用/游戏两榜,停用即从发布表单隐藏 |
| 敏感词库 | CRUD+等级调整,`Filter.Invalidate()` 热更新,无需重启 |
| AI 管家 FAQ | 词条 CRUD,botReply 实时查库即时生效 |
| 协议管理 | 用户协议/隐私政策在线编辑,客户端免登录拉取 |
| 账号管理 | 建号/角色分配/停用/重置密码/强制解绑 TOTP,禁止自操作;角色:超管/审核员/运营 |
| 安全设置 | TOTP 绑定引导(密钥/otpauth/恢复码)与解绑 |
| 操作日志 | 全部敏感操作留痕 admin_op_log |

## 四、部署与运维手册

### 本地/联调(docker compose)

```powershell
cd server
docker compose up -d --build      # MySQL(13306)/Redis(6379)/API(8888)/WS(8889),迁移自动执行
cd ..\admin-web
npm install; npm run dev          # http://localhost:5173,初始 admin/admin123(首登强制改密)
```

### 生产(docker-compose.prod.yml)

1. `cp .env.prod.example .env.prod` 填齐:JWT 密钥、MySQL/Redis 密码、SMTP、`YIORA_ADMIN_IP_ALLOWLIST`(逗号分隔 IP/CIDR,留空不限)。
2. `docker compose -f docker-compose.prod.yml --env-file .env.prod up -d`,镜像来自 GHCR(CI 自动发布),Caddy 终结 TLS 并反代 api/ws,内网隔离 `/internal/push`。
3. `admin-web`:`npm run build` 产物 `dist/` 托管到任意静态服务,反代 `/admin/v1` 到 api。

### 发版前回归基线

```powershell
cd server; powershell -File .\scripts\smoke.ps1
# down -v 重置 → 起栈 → 依次跑 community/software/m3/mall/paid-ai/content/account/p1/p2/admin 十套件
# 期望输出 SMOKE RESULT: ALL PASSED
```

CI(GitHub Actions):build + vet + 单测(-race) + testcontainers 并发集成测试(忧珠/解锁/抽奖) + Docker 镜像发布。

### 常用 runbook

| 场景 | 操作 |
| --- | --- |
| 管理员被错密锁定 | `docker exec yiora-redis-1 redis-cli DEL admin:login:fail:<username>` |
| 管理员忘记密码 | 超管在「账号管理」重置(对方首登强制改密);超管自己忘 → UPDATE admin_user 写入新 bcrypt 并置 must_change_pwd=1 |
| 敏感词误拦 | 后台「敏感词库」改等级/删除,保存即热更新 |
| 忧珠账不平 | api 每日对账 goroutine 打 ERROR 日志;按 youzhu_log(幂等 biz_key)与 youzhu_account 差额定位 |
| 新增迁移 | sql/ 按序号追加,dev 重置卷自动执行;生产对已有库手动执行增量文件 |

## 五、质量基线

- 冒烟:十套件 ≈ 90 组断言,覆盖注册→发帖→评论→私信→举报→审核→处置→运营配置全链路(含验证码/锁定/热更新等安全路径)。
- 单测:敏感词过滤、幂等点赞、IM seq、JWT、任务结算、IP 白名单等 12+;集成:真实 MySQL 并发(50 并发记账/解锁/抽奖,死锁重试验证)。
- admin-web:vue-tsc 严格模式零错误。

## 六、已知限制与二期路线

| 项 | 现状 | 二期建议 |
| --- | --- | --- |
| 搜索 | Meilisearch 驱动已落地(compose 默认启用,prod 按需开) | 搜索联想/关键词高亮 |
| 公告扇出 | 单 SQL 全员插入 | 十万级用户改 MQ 分批(代码处已标注) |
| 敏感词匹配 | 朴素子串 O(词数×文本长) | 词库过万换 Aho-Corasick |
| 图片机审 | 骨架+mock 驱动已落地(异步送审/自动拦截/工作台展示) | 接腾讯 IMS 真驱动(密钥到位即接,见调研文档) |
| AI 管家 | 关键词规则 | 接大模型(botReply 单点替换) |
| 通知推送 | 站内信+WS 在线推 | 接 APNs/厂商通道离线推送 |
| 帖子分享 | 口令生成/解析+计数已落地 | 分享图模板、站内转发流 |
| 青少年模式 | 开关+消费拦截(付费/抽奖/兑换)已落地 | 内容池+时长限制 |
| APK 大文件 | Multipart 分片+断点续传已落地(2GB) | 帖子视频复用同通道 |
| Flutter 客户端 | 骨架进行中(app/) | 按需求文档 M2-M4 页面逐屏落地 |

已在 v1.1/v1.2 落地并从限制表移除:对象存储直传、TOTP 二步验证、双令牌+设备管理、
后台圈子/话题/忧珠/靓号/协议/等级头衔运营能力、看板趋势图。
